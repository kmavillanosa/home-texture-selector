# Optional: Override for local development without Docker
# Copy to docker-compose.override.yml and customize as needed

# Example: Use localhost API when running app outside Docker
# services:
#   app:
#     environment:
#       - API_HOST=host.docker.internal  # Windows/Mac
#       # - API_HOST=172.17.0.1  # Linux alternative
