services:
  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    ports:
      - "3001:3000"
    volumes:
      - api-uploads:/app/uploads
      - api-cache:/app/cache
    environment:
      - PORT=3000

volumes:
  api-uploads:
  api-cache:
